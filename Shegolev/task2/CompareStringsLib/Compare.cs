using System;
using System.Collections.Generic;

namespace CompareStringsLib
{
    public class Compare
    {
        public static void Sort(string str)
        {
            int L = 0;
            
            List<string> list = new List<string>();
            list.Add(str);
            list.Count();
        }   
    }
}
